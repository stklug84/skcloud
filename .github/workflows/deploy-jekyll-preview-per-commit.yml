---
# title: Deploy per-commit preview workflow
# authors:
#   - Steffen Klug <45033201+stklug84@users.noreply.github.com>
# date: 2026-06-22
# keywords: [infrastructure, jekyll, deploy]
# comment: |
#   Per-commit preview deploy to blutoniumstrom.com. Build/stage/prune/
#   publish logic lives in the central reusable workflow:
#     stklug84/github-workflows/.github/workflows/jekyll-deploy-preview.yml
#   This caller owns the trigger, permissions, concurrency, and secrets.
# ---
name: Deploy per-commit preview to blutoniumstrom.com

on:
  push:
    branches: ['**']
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: preview-${{ github.ref }}
  cancel-in-progress: false

jobs:
  preview:
    uses: stklug84/github-workflows/.github/workflows/jekyll-deploy-preview.yml@74e332dac4787b3f4f9fe2f896a6a0b3a03b9385  # v1.20.0
    with:
      previews-repo: blutoniumstrom/blutoniumstrom.github.io
      preview-domain: https://blutoniumstrom.com
    secrets:
      previews-deploy-token: ${{ secrets.PREVIEWS_DEPLOY_TOKEN }}
